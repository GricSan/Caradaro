<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/shape_rectangle_top_round_16dp"
    android:padding="16dp">

    <ImageView
        android:id="@+id/iv_vehicle_photo"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:src="@color/black"
        app:layout_constraintBottom_toBottomOf="@id/tv_vehicle_color"
        app:layout_constraintDimensionRatio="1:1"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@id/tv_vehicle_name" />

    <TextView
        android:id="@+id/tv_vehicle_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:text="Vehicle name"
        app:layout_constraintStart_toEndOf="@id/iv_vehicle_photo"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/tv_vehicle_address"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:layout_marginTop="4dp"
        android:layout_marginBottom="4dp"
        android:text="Vehicle address"
        app:layout_constraintBottom_toTopOf="@id/tv_vehicle_color"
        app:layout_constraintStart_toEndOf="@id/iv_vehicle_photo"
        app:layout_constraintTop_toBottomOf="@id/tv_vehicle_name" />

    <TextView
        android:id="@+id/tv_vehicle_color"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:text="Vehicle color"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toEndOf="@id/iv_vehicle_photo" />

</androidx.constraintlayout.widget.ConstraintLayout>